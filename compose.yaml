services:
  api:
    build: .
    ports: ["8000:8000"]
    env_file: [.env]
    volumes: [chroma-data:/app/chroma_db, upload-data:/app/uploads]
    restart: unless-stopped
volumes:
  chroma-data:
  upload-data:

# Operations

Terminate TLS at ingress; require authentication and authorization; encrypt persistent
volumes; restrict egress; inject secrets through a manager; define retention; monitor
latency, retrieval quality, errors, and storage; test backups; review provider terms.

`main` is releasable. Use short-lived feature, fix, docs, test, and chore branches.
Require focused PRs, Conventional Commits, automated checks, and review.
